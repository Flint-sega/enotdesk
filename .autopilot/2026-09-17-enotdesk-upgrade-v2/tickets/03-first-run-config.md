# 03 — Первый запуск: вшитый адрес, файл-конфиг, экран соединения

**Требования:** R03, R04, R05
**Blocked by:** —
**Зона:** `client/lib/first-run.mjs` (новое), `client/main.mjs`, `client/renderer/settings.js`, `client/renderer/index.html`, `client/locales/*`, `docs/BUILD.md`
**Волна:** 1
**Status:** ready

## Что должно заработать

Клиент настраивается без ручного ввода: адрес вшит при сборке ИЛИ лежит файлом
рядом с exe. Первый запуск честно показывает: сервер доступен или нет, кнопкой
«Проверить». На главном экране — чип статуса сервера.

## Из брифа, дословно

> «B2» (вшитый адрес) · «B1» (файл-конфиг рядом с portable-exe) ·
> «B3» (экран проверки соединения)

## Разделы спецификации

Истории 5–7, Решения §первый запуск.

## Критерии приёмки

- [ ] `client/lib/first-run.mjs`: `resolveServerUrl({saved, execPath, baked}) → {url, source}` — порядок: saved → файл `enotdesk-server.txt` рядом с exe → baked → дефолт; юнит-тесты всех веток и мусора в файле
- [ ] B2: `ENOT_BAKED_SERVER_URL` при сборке попадает в клиент (electron-builder extraMetadata или конфиг), если задан
- [ ] B3: экран статуса с кнопкой «Проверить» (health через существующий мост), чип статуса на главном экране; тексты — словари ru/en
- [ ] `npm test`, `npm run lint` зелёные; контракт-тесты (id/ключи) обновлены
