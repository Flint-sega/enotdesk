# Изначальная задача

> Записано 2026-09-17. Текст задачи не редактируется — он эталон, с которым
> сверяется готовый результат. Всё, что сказано позже, дописывается
> в «Дополнения» ниже.

1. «пока не проверял, подумай как можно улучшить программы для клиентов,
   например уменьшить размер или добавить что-то подумай и дай варианты»

2. «A1, A2 подумай как избежать проблем в дальнейшем если не избежать то
   не делаем, A3 , B1, и B2, B3 , С1 , С2, С4, С5, D1, D, D»
   (A1 — срез чужеплатформенных бинарников koffi; A2 — чистка ресурсов Electron;
   A3 — адаптивный битрейт видео; B1 — файл-конфиг сервера рядом с portable-exe;
   B2 — вшитый адрес сервера при сборке; B3 — экран проверки соединения;
   C1 — удалённый терминал на машину; C2 — админ-UI машин; C4 — сообщение
   на экран машины; C5 — инвентарь машин; D1 — webhooks; D2 — TOTP-2FA;
   D3 — запись сеансов)

3. «сделай все это сам и запуш релизы» (предыдущий прогон — публикация)

## Дополнения

- 2026-09-17 — по вариантам улучшений выбрано: A1, A3, B1, B2, B3, C1, C2, C4,
  C5, D1, D2. A2 — «подумай как избежать проблем в дальнейшем; если не
  избежать — не делаем» (разведка показала: безопасный срез ~1–2 МБ,
  агрессивная хирургия Framework не делается).
- 2026-09-17 — развилки: блок D = «D1 + D2 (Рекомендую)» (запись сеансов D3
  не делаем); терминал C1 = «От службы, SYSTEM (Рекомендую)»; этапность =
  «Один прогон (Рекомендую)».
- 2026-09-17 — план «Прогон клиентский апгрейд v2 — 2 фазы, 11 тасков»
  одобрен.
