# 05 — Лёгкая сборка, быстрый старт, документация

**Требования:** R03, R04
**Blocked by:** 01, 03
**Зона:** `build/electron-builder.yml`, `docs/BUILD.md`, `README.md`
**Волна:** 3

## Что должно заработать

Сборка стала легче, старт — не медленнее; всё измерено фактически; документация отражает новую структуру и цифры.

## Критерии приёмки

- [ ] `build/electron-builder.yml`: `compression: maximum`, `electronLanguages: [ru, en]`, `files` — только client + нужные ассеты + package.json, `asarUnpack` — только koffi; сборка `npm run pack:mac` успешна
- [ ] Замер до/после: `stat` размера `dist/EnotDesk-mac-arm64.zip` (до — зафиксировать из предыдущей сборки), результат в отчёте; цель — минус ≥5 % или честное объяснение, что упёрлись (Electron-ядро)
- [ ] Старт: в `EDESK_SMOKE` лог времени от запуска процесса до `did-finish-load`/создания окна; замер после оптимизаций не хуже прежнего более чем на 10 % (прежнее значение замерить до правок, если нет — измерить сразу и сравнить с разумной базой, указав метод)
- [ ] `docs/BUILD.md` и README обновлены: структура папок, команды pack/start/test, честные размеры/цифры, macOS-проверено, win/linux — нет
- [ ] `npm test` зелёный; смоук и pack:mac фактически прогнаны

## Из брифа, дословно

> «чтоб приложение весело не много чтоб клиент его скачал быстро и запускалось быстро»
