# 03 — Визуал приложения по эталону

**Требования:** R01, R04, R06, R08i, R10i
**Blocked by:** 01, 02
**Зона:** `client/`
**Волна:** 2

## Что должно заработать

Главный экран клиента выглядит как reference/app.png: шапка, hero с маскотом, крупная CTA, плитки, футер; весь функционал и все состояния работают как раньше.

## Критерии приёмки

- [ ] Шапка: иконка-логотип (`enot-icon.svg`), «EnotDesk | Удалённая поддержка», справа кнопки-иконки «Помощь» и «Оператор» (таб оператора сохраняется — отступление от эталона ради R04, отмечено), шестерёнка настроек; sticky с лёгким блюром
- [ ] Hero: eyebrow «УДАЛЁННАЯ ПОДДЕРЖКА», H1 «EnotDesk», подзаголовок «Быстрая и безопасная помощь на расстоянии», пояснение про ID/пароль, крупная teal-CTA «Получить помощь» (иконка отправки + стрелка), строка с замком «Соединение устанавливается только с вашего согласия»
- [ ] Справа маскот `../../assets/mascot-app.png` в круге teal-свечения — подпись внутри растра, не дублировать
- [ ] Низ: три плитки Безопасно/Быстро/Удобно (щит/молния/люди) с описаниями; футер «EnotDesk v<version>» + «С заботой о ваших задачах ♥»
- [ ] `getSettings()` отдаёт `version` (`app.getVersion()`); футер не выводится при отсутствии версии
- [ ] Все прежние id/классы на месте (программная сверка со списком из app.js), все состояния клиента/оператора/настроек работают; CSP без inline — стили только в styles.css
- [ ] Ленивая загрузка koffi: `permissions()` не грузит нативный модуль, загрузка — при первом host-сеансе; тест на шве (status/permissions без загрузки) + существующие тесты зелёные
- [ ] `npm test` зелёный; Electron-смоук: скриншот главного окна переснят и просмотрен

## Из брифа, дословно

> «изменяем визуал … приложения … удалёнкапрограмма.png … функционал сохраняем»
