# 06 — C5: инвентарь машин

**Требования:** R06
**Blocked by:** —
**Зона:** `server/machines.mjs` (inventory в store), `server/db.mjs` (миграция), `server/app.mjs` (heartbeat-приём + выдача), `client/lib/agent.mjs` + `client/main.mjs` (сбор и отправка), `server/test/machines.test.mjs`
**Волна:** 4
**Status:** ready

## Что должно заработать

Агент присылает с heartbeat инвентарь (ОС, версия клиента, uptime процесса,
свободное место) — админ видит это на машине.

## Из брифа, дословно

> «C5» — инвентарь машин (os/version/uptime/диск)

## Разделы спецификации

История 8, Решения §инвентарь.

## Критерии приёмки

- [ ] Heartbeat расширен полем inventory (JSON, ограничен 4 КБ); store сохраняет и отдаёт вместе с машиной
- [ ] Валидация: строки ограничены, числа числа — мусор отбрасывается
- [ ] Тесты: heartbeat с инвентарём → сохранён и виден в API; мусор отброшен; без инвентаря — не падает
- [ ] statfs по платформам честно: где недоступно — поле отсутствует, не фейк
