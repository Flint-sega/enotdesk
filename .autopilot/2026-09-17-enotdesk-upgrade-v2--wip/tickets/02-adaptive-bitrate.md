# 02 — A3: адаптивный битрейт видео

**Требования:** R02
**Blocked by:** —
**Зона:** `client/lib/adaptive-bitrate.mjs` (новое), `client/renderer/session-media.js`, `client/test/adaptive-bitrate.test.mjs` (новое)
**Волна:** 1
**Status:** ready

## Что должно заработать

На плохой сети видео плавно падает по качеству (2500→1200→600 кбит/с), на
хорошей — восстанавливается, без дёрганья туда-сюда (гистерезис 15 с).

## Из брифа, дословно

> «A3» · «на плохой сети видео деградирует плавно, а не рассыпается»

## Разделы спецификации

Истории 3–4, Решения §битрейт.

## Критерии приёмки

- [ ] `nextTarget({lossPct, rttMs}, current, lastChangeAt, now) → {target, changed}` — чистая, без DOM
- [ ] Понижение: потери ≥5% (или rtt>400) → следующая ступень вниз; восстановление: потери <2% и rtt<150, не раньше 15 с после понижения
- [ ] Юнит-тесты: ступени, гистерезис (раньше 15 с не поднимает), восстановление, грани (NaN/отсутствие stats)
- [ ] Интеграция: quality-polling в session-media применяет target через существующий setParameters
