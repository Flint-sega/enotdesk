# 08 — C4: сообщение на экран машины

**Требования:** R08
**Blocked by:** 07 (панель машин в web)
**Зона:** `client/lib/notify.mjs` (новое), `client/lib/agent.mjs`/`client/main.mjs` (приём notify с машины → вызов notify), `web/operator.*` (кнопка/поле в панели машин), `client/locales/*`, `client/test/notify.test.mjs` (новое)
**Волна:** 5
**Status:** ready

## Что должно заработать

Оператор печатает текст — на экране машины появляется системное сообщение.
Windows: WTSSendMessageW в активную сессию (главный путь). Linux/macOS:
notify-send/osascript best-effort. Каждая ОС — честный статус результата.

## Из брифа, дословно

> «C4» — сообщение на экран машины (Windows WTSSendMessage — главный путь)

## Разделы спецификации

История 10, Решения §toast.

## Критерии приёмки

- [ ] `showToast(platform, text) → {ok, reason?}` — чистая логика + платформенный вызов; юнит-тесты на моках (win/mac/linux/неизвестная ОС)
- [ ] Windows: WTSSendMessageW через koffi (wtsapi32), сервер не нужен; лимит текста честный
- [ ] Оператор: поле+кнопка в панели машин, статус результата; клиентская сторона (агент) получает notify → показывает toast → отвечает результатом
- [ ] Тексты словарями ru/en; непокрытые платформы — MANUAL-QA
