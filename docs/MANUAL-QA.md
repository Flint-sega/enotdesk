# EnotDesk — чек-лист ручной приёмки

Этот чек-лист — для живой приёмки на двух реальных машинах. Автотесты (135+ unit/server) не
покрывают нативный ввод, WebRTC-медиу и поведение служб на живых ОС — здесь фиксируем именно это.

Правила:
- **Приоритет — Windows** (решение владельца: «Все три, приоритет Win»).
- Чек-лист **пополняется фазами**: после приёмки фазы добавляются новые пункты, старые не удаляются.
- Отметка `[x]` = проверено живьём, дата и результат (комментарий) обязательны. Пункт, который
  нельзя проверить — честно помечается «N/A + причина», не «пройдено».
- Подготовка: свежие сборки на обеих машинах (один и тот же билд), сервер развёрнут и доступен,
  админ + оператор заведены. Ссылки: `docs/AGENT.md`, `docs/BUILD.md`, `docs/SERVER.md`.

## Фаза 1 — Windows, attended (приоритет)

| ID | Проверка | Ожидаемо | Результат |
|----|----------|----------|-----------|
| W-A1 | Установка и первый запуск клиента (SmartScreen) | окно открывается; предупреждение SmartScreen обходится «Выполнить в любом случае» (инструкция README) | |
| W-A2 | Приглашение: клиент → sessionId+пароль, оператор → claim | подключение, у клиента согласие на доступ | |
| W-A3 | Согласие и старт сессии | видео экрана транслируется, таймер/качество видны | |
| W-A4 | Клавиатура оператора (в т.ч. не-латинская раскладка у оператора) | символы совпадают с физическими клавишами хоста (коды e.code) | |
| W-A5 | Скролл в строках и списках | плавный скролл обеими сторонами | |
| W-A6 | Чат и буфер обмена в сессии | сообщения туда-обратно, вставка текста на хосте | |
| W-A7 | Передача файлов в сессии (кнопкой и drag&drop) | «Принять» у клиента, файл сохраняется, лимиты размеров работают | |
| W-A8 | Индикатор качества / смена экрана (replaceTrack) / пауза трансляции | всё отражается у оператора без переподключения | |
| W-A9 | Обрыв сети на ~10с и возврат | грейс 30с (ADR 0013): сессия выживает без повторного согласия | |
| W-A10 | Закрытие окна клиента на хосте | сессия завершена, ввод у оператора тут же мёртв (fail-closed), сеанс в /history | |
| W-A11 | Повторный запуск клиента | один экземпляр (второй не стартует; EDESK_ALLOW_MULTI=1 — только тестовый обход) | |

## Фаза 2 — Windows, unattended (приоритет)

| ID | Проверка | Ожидаемо | Результат |
|----|----------|----------|-----------|
| W-U1 | Регистрация машины по onboarding-коду | машина появилась в списке, статус online; повторное использование кода отклонено | |
| W-U2 | Установка службы (`windows-install.bat`) | служба `EnotDeskAgent` создана (start= auto, описание на месте), машина online после `sc start` | |
| W-U3 | Ребут машины без человека | служба поднялась сама, машина снова online (история 24) | |
| W-U4 | Убийство процесса агента | SCM перезапускает (failure actions 5с/10с/30с) | |
| W-U5 | Claim из службы оператором с причиной, PIN-группа | причина обязательна (без причины отказ), PIN запрошен, audit: unattended=true | |
| W-U6 | Чат/файлы в unattended-сессии | работают через DataChannel, у оператора виден индикатор непрочитанного чата | |
| W-U7 | **Session 0 — ожидаемое ограничение v1** (история 31) | после claim: машина online, чат/файлы работают; видео консоли отсутствует/чёрное, ввод в консоль не действует — **это ожидаемое поведение, не дефект**; факт зафиксирован в отчёте приёмки | |
| W-U8 | Логи службы Windows | подтверждено честное ограничение v1: файловых логов нет; диагностика через консольный запуск с EDESK_AGENT=1 работает | |
| W-U9 | Отзыв/удаление машины на сервере | агент-служба больше не регистрируется (токен отозван), после удаления `agent-token.json` — новый код регистрирует заново | |
| W-U10 | **Агент v1 без tray-иконки — сознательное решение**: в трее нет иконки; управление — через службы ОС (`services.msc`, `sc.exe`), статус машины — на сервере | отсутствие иконки не регистрируется как дефект; управление службой по docs/AGENT.md работает | |
| W-U11 | **Живой терминал** (история 11, R09): в сеансе с машиной открыть панель «Терминал машины», выполнить `whoami` / `hostname` | панель получает `opened` с честным контекстом (на Windows — SYSTEM), команды выполняются, вывод возвращается; повторное открытие — отказ `term-busy`; простой 5 мин закрывает; audit содержит `term.open`/`term.close` по переходам | |
| W-U12 | **Сообщение на экран машины** (история 10, R08): из панели машин отправить тост на живую Windows-машину | диалог WTSSendMessageW появляется на экране **активной консольной сессии**; подтверждение у оператора после нажатия OK; если машина стоит на экране входа (активной сессии нет) — оператор получает честный отказ `no-active-session` | |
| W-U13 | Живая проверка ephemeral TURN через coturn: сервер с `ENOT_TURN_SECRET`, оператор подключается к машине вне LAN | coturn принимает эфемерный кред (HMAC-SHA1, TTL 1 ч) из `/rtc-config`, видеосеанс идёт через TURN-релей; без секрета — прежнее статическое поведение | |

## Фаза 3 — Linux, unattended

| ID | Проверка | Ожидаемо | Результат |
|----|----------|----------|-----------|
| L1 | `install-linux.sh` | пользователь/профиль/env/unit созданы, служба enabled, машина online | |
| L2 | Хардненинг unit | `systemd-analyze security enotdesk-agent` без критичных оверрайдов; агент пишет только в ReadWritePaths | |
| L3 | Ребут и убийство процесса | Restart=always поднимает; машина online после ребута | |
| L4 | Ввод в X11-сессию (DISPLAY/XAUTHORITY настроены) | клавиатура/скролл доходят до консоли | |
| L5 | Wayland-сессия | агент работает, ввод честно `wayland-unsupported-control` (не падает) | |
| L6 | Обновление сборки (stop → заменить → start) | машина возвращается online, токен сохранён | |
| L7 | `remove-linux.sh [--purge-data]` | служба/файлы/пользователь удалены согласно флагам | |

## Фаза 4 — macOS, unattended

| ID | Проверка | Ожидаемо | Результат |
|----|----------|----------|-----------|
| M1 | `install-macos.sh` | LaunchDaemon загружен (`launchctl print`), лог пишется в /var/log/enotdesk-agent.log, машина online | |
| M2 | Ребут и убийство процесса | KeepAlive поднимает; машина online после ребута (старт до логина) | |
| M3 | **TCC-разрешения службы от root** | зафиксировать фактическое поведение: захват/ввод либо работают, либо честно сообщают об отсутствии разрешения (MDM-профиль — отдельным пунктом отчёта) | |
| M4 | Ввод в консольную сессию после выдачи Accessibility | клавиатура/скролл доходят | |
| M5 | Обновление (bootout → заменить .app → bootstrap) | машина возвращается online, токен сохранён | |
| M6 | `remove-macos.sh [--purge-data]` | daemon выгружен, файлы удалены согласно флагам | |

## Известные ограничения v1 (не дефекты при приёмке)

- Windows: session 0 — видео/ввод консольной сессии из службы недоступны (W-U7, docs/AGENT.md).
- Тост на экран: идёт только в активную консольную сессию; машина на экране входа — отказ `no-active-session` (W-U12). Второй тост заменяет незабранный агентом первый (у оператора, чей тост заменили, — «подтверждения нет»).
- Терминал: мост поднимается с `iceServers: []`, если `/rtc-config` недоступен или TURN не настроен — соединение только по LAN/напрямую; сквозной терминал через NAT — проверить вручную (W-U11).
- Windows: файловых логов у службы нет (W-U8).
- Агент v1 без tray-иконки — сознательное решение, управление через службу (W-U10).
- Wayland (Linux): управление вводом не поддерживается, честный статус в UI/логе.
- macOS: TCC для LaunchDaemon без MDM ограничен (M3).
- Сборки неподписаны: SmartScreen/Gatekeeper — обход по README (история 35).

## Прогон upgrade-v2 (новые фичи — живая приёмка)

- B3/B1/B2 — первый запуск: baked-адрес (сборка с `-c.extraMetadata.ENOT_BAKED_SERVER_URL=...`) → чип «Сервер доступен»; файл `enotdesk-server.txt` рядом с exe подхватывается; экран проверки показывает честный статус
- Инвентарь — heartbeat несёт ОС/версию/uptime/диск → панель «Машины» показывает; мусор отбрасывается
- Адаптивный битрейт — при потерях ≥5% видео ступенчато падает (2500→1200→600), при восстановлении растёт не раньше 15 с
- TOTP-2FA — включение (ENOT_SECRET_KEY задан, QR/секрет, 10 резервных кодов), логин с кодом, неверный код, резервный код, отключение с паролем
- Webhooks — session.started/ended + machine.claim.denied доходят с верной подписью; неверный секрет/URL → ретраи и тишина
- Терминал (W-U11) — живой сквозной прогон через NAT: iceServers из /rtc-config доезжают до моста
- Toast (W-U12) — живой WTSSendMessageW: тост в активной сессии; экран входа → no-active-session
- quick-setup — `scripts/quick-setup.sh` на чистой VM: bare-metal (с доменом и без: HTTP-предупреждение, DNS-подсказка), `--docker`, автоген-пароль показан один раз и работает, повторный запуск без `--update` честно отказывает, `--update` сохраняет секреты и подхватывает умолчания из env-файла

## EnotDesk Hub (живая приёмка)

Хаб (тикет-система + чат-виджет + one-click + email-канал, ADR 0023–0025) покрыт автотестами
на уровне API/швов; ниже — живая приёмка: реальный сайт, реальный почтовый ящик, три ОС.
Подготовка: сервер EnotDesk развёрнут, хаб включён и отдаётся наружу (bare-metal — юнит
`enotdesk-hub` за Caddy, docker — профиль `hub`), в консоли есть аккаунты admin и operator
(EnotDesk-аккаунты; SSO), виджет добавлен на реальный сайт. Консоль хаба — `/hub/`, админ-раздел
настроек — в ней же.

### Виджет на реальном сайте

| ID | Проверка | Ожидаемо | Результат |
|----|----------|----------|-----------|
| H-S1 | Embed-строка на сайте: `<script src="https://hub.example/widget.js" data-server="https://hub.example"></script>` | пузырь внизу справа; клик открывает iframe `/w` того же origin (Caddy-маршрут); на мобильной ширине чат раскрывается на весь экран | |
| H-S2 | В админ-настройках виджета задать origins (hostname сайта, ≤20), consentRequired + policyUrl | настройки сохраняются; запросы виджета с чужого Origin получают `403 cors_denied` (проверить curl'ом с `Origin:` вне allowlist), со своего — работают | 24–25.09.2026, частично (support.ruenot.site): same-origin — 200, чужой Origin — 403 `cors_denied`, без Origin — 403; consent-галка сохраняется в UI. Эхо домена из allowlist не проверено — нет стороннего сайта (H-S1) |
| H-S3 | Pre-chat: первое открытие чата | форма имя/email (оба необязательны); при consentRequired=1 — чекбокс согласия со ссылкой на policyUrl; отправка без галочки — честная ошибка согласия (`403 consent_required` на API), после галочки — сообщение уходит, тред `chat` появляется в инбоксе | 25.09.2026, частично: consent-гейт проверен на API — append без согласия `403 consent_required`, с `consent:true` — 201 + тред chat; pre-chat-форма в UI под новым гостем не прогонялась |
| H-S4 | Offline → тикет: агенты offline (никто не онлайн в консоли) | виджет показывает индикатор offline и форму (email, тема, сообщение); отправка создаёт тикет `channel=email` в инбоксе, гостю — экран «принято» | 25.09.2026: агент offline → виджет «We are offline»; offline-заявка (`/api/hub/widget/offline`, visitorId сгенерирован сервером) → тикет `channel=email` с email контакта в инбоксе. Форма в UI отрисована, submit формы — через API |
| H-S5 | История по visitor-cookie: перезагрузить страницу сайта (или закрыть/открыть вкладку) | диалог и история сообщений восстанавливаются (visitor-cookie `enot_wv` хаба + visitorId в localStorage); в чистом браузере/после очистки cookie — история пуста, это новый гость | 25.09.2026: перезагрузка тестовой страницы — диалог и вся история (3 сообщения + карточка one-click) восстановились |
| H-S6 | Rating после resolve: агент завершает тикет («Resolved») | в виджете появляются звёзды; оценка 1–5 сохраняется (при повторном открытии — «спасибо», звёзды неактивны); на треде в консоли видна оценка; неготовый (не resolved) тред оценить нельзя | 25.09.2026: resolve → звёзды в виджете → 5/5 → «Thanks for your feedback», звёзды неактивны; в консоли «Chat rating: 5 of 5» (в открытой панели — после переоткрытия треда); следующее сообщение гостя открыло новый тред |

### Консоль агента в браузере

| ID | Проверка | Ожидаемо | Результат |
|----|----------|----------|-----------|
| H-C1 | Логин на `/hub/` через EnotDesk-аккаунт (SSO) | вход обычным логином/паролем; у аккаунта с включённой TOTP-2FA хаб запрашивает код (`totp_required` проходит насквозь); роль auditor получает страницу 403; повторный вход в соседней вкладке — та же сессия по cookie | 24–25.09.2026, частично: SSO-вход админом — ок; auditor: SSO проходит, API консоли — 403. TOTP-passthrough не проверен (2FA не включалась), сессия в соседней вкладке не проверялась |
| H-C2 | Инбокс и фильтры | список тредов с пагинацией; фильтры по статусу (open/pending/resolved), каналу (chat/email/manual), тегу и поиску работают по отдельности и вместе; пустой отфильтрованный список честно подписан | 25.09.2026, частично: треды появляются живьём (WS), фильтр по каналу Email/All работает; фильтры статуса/тега/поиска и пагинация не прогонялись |
| H-C3 | Canned-подсказки: в поле ответа набрать `#` | попап шорткатов; shared-подсказки видны всем агентам, private — только автору; выбор вставляет текст в ответ | 25.09.2026, частично: `#` → попап (пусто — честное «No canned replies found»); шаблон создан через API (window.prompt подавлен в среде автоматизации) — подсказка `#greet` появилась, выбор вставил текст, отправка прошла; shared/private разделение не проверено |
| H-C4 | Теги | добавление/удаление тег-чипов на треде (≤10, каждый ≤30); фильтр инбокса по тегу находит тред | 25.09.2026, частично: тег «тест» добавлен и удалён через UI; фильтр инбокса по тегу не прогонялся |
| H-C5 | Presence: агент переключает статус (online/away/offline) в консоли | индикатор присутствия в виджете обновляется без перезагрузки страницы (WS); все агенты offline → виджет показывает offline-форму (см. H-S4) | 25.09.2026: online → виджет «We are online»; offline → «We are offline» + offline-режим. Переключение проверено с перезагрузкой страницы виджета; live-пуш статуса без reload не изолирован |

### One-click: протокол enotdesk:// (упакованный клиент, 3 ОС)

| ID | Проверка | Ожидаемо | Результат |
|----|----------|----------|-----------|
| H-J1 | Агент отправляет карточку «Подключиться» в тред (кнопка в консоли) | гостю в виджет и в тред приходит карточка `remote-offer` со ссылкой `enotdesk://join?server=…&t=…` и страницей `/join?t=…`; токен одноразовый, TTL 10 мин | 25.09.2026: карточка доставлена и в тред, и в виджет (кнопки «Launch helper» / «Client page»); TTL не ждали |
| H-J2 | Windows: клик по карточке на машине с установленным (упакованным) клиентом | протокол открывает клиент (регистрация в реестре); клиент стартует обычный ожидающий сеанс и сам репортит `{sessionId,password}` на хаб; хаб делает авто-claim bearer'ом агента, в треде — system-сообщение о сеансе; оператор подключается как в attended-цикле | живые машины — по команде владельца |
| H-J3 | macOS: то же (`open-url` даже при закрытом приложении) | аналогично H-J2; повторный клик при запущенном клиенте доходит через second-instance | живые машины — по команде владельца |
| H-J4 | Linux: то же (MimeType x-scheme-handler в .desktop из сборки) | аналогично H-J2 | живые машины — по команде владельца |
| H-J5 | Fallback `/join?t=…`: открыть страницу из карточки в браузере без клиента | страница «Подключиться» с кнопкой протокола и кнопкой скачивания (ведёт на `/downloads`); после установки клиентом клик по кнопке протокола завершает сценарий H-J2 | 25.09.2026: страница живая — кнопка протокола (`enotdesk://join?…`) + «Download the app» → `/downloads` |
| H-J6 | Одноразовость: повторно использовать токен (ссылка из чата после успеха или по истечении 10 мин) | повторный репорт — честный отказ (токен уже использован/истёк); страница `/join?t=…` с погашенным/несуществующим токеном — честная 404, кнопок нет | 25.09.2026, частично: репорт с мусорным телом — 400 и токен НЕ сожжён (страница `/join?t=` жива); несуществующий токен — 404. Повторный репорт после успеха — нужен упакованный клиент (H-J2) |

### Email-канал (реальный IMAP-ящик + SMTP)

| ID | Проверка | Ожидаемо | Результат |
|----|----------|----------|-----------|
| H-E1 | Включение канала в настройках (admin): imap `{host,port,tls,user,pass}` + smtp `{…,from}` | сохраняется; маска GET показывает `hasPass`, пароля нигде нет; без `ENOT_SECRET_KEY` включение честно отказывает (`secret_key_missing` — пароли хранятся только шифротекстом); пустое поле пароля = «оставить прежний» | |
| H-E2 | «Проверить связь» | IMAP connect+INBOX и SMTP verify проверяются по отдельности; каждая сторона честно `ok` или `error` с текстом; результат виден в настройках | |
| H-E3 | Письмо → тикет: отправить письмо на ящик с внешнего адреса | в ≤1 мин (тик поллера) в инбоксе тикет `channel=email`: тема, текст, отправитель; письмо помечено прочитанным и не создаёт дублей при следующем опросе | |
| H-E4 | Ответ агенту в консоли → письмо клиенту | письмо доходит от `smtp.from` с темой `Re: …` и заголовком `In-Reply-To` последнего Message-ID треда; сбой доставки — честное system-сообщение в треде | |
| H-E5 | Ответ клиента на письмо агента | ответ цепляется в тот же тред (threading по In-Reply-To/References), а не создаёт новый тикет | |
| H-E6 | Анти-петля: письмо с собственного адреса (`smtp.from`/`imap.user`) и заголовком `Auto-Submitted` ≠ `no` (bounce, автоответ) | тикет не создаётся (письмо помечается виденным и не возвращается); письмо человека с того же адреса без заголовка — тикет создаётся | |

### Webhooks EnotDesk → хаб

| ID | Проверка | Ожидаемо | Результат |
|----|----------|----------|-----------|
| H-W1 | Секрет из консоли хаба (admin-раздел) скопировать в настройки webhooks сервера EnotDesk: URL `https://hub.example/hooks/enotdesk`, события `session.started` + `session.ended` | секрет совпадает; после смены `ENOT_SECRET_KEY` хаб показывает rotated-секрет — админ обязан обновить его на стороне сервера, иначе доставка падает по подписи | 25.09.2026: секрет совпадает с консольным, webhooks сервера настроены (`https://support.ruenot.site/hooks/enotdesk`, оба события); ротация ключа не прогонялась |
| H-W2 | Живой one-click сеанс из треда (H-J2) | в треде появляется system-сообщение «сеанс начат» (session.started), после завершения — «сеанс завершён» с причиной (session.ended); события от сеансов без join-токена хаб молча игнорирует | живые машины — по команде владельца |
| H-W3 | Неверный секрет/подпись | `/hooks/enotdesk` отвечает `401 bad_signature`, тикет не трогается; серверные ретраи видны в логах сервера, сеанс не блокируется | 25.09.2026: `401 bad_signature` на мусорную подпись и на подпись чужим ключом (после добавления `/hooks/enotdesk` в Caddy-маршруты хаба — фикс 91026cd) |

### Docker-профиль hub

| ID | Проверка | Ожидаемо | Результат |
|----|----------|----------|-----------|
| H-D1 | `.env`: `TURN_SECRET`, `ENOT_SECRET_KEY`, `HUB=1`, `COMPOSE_PROFILES=hub`; `cd docker && docker compose up -d` | поднимается контейнер `hub` (порт 8090 наружу НЕ опубликован) после health сервера; БД — том `hub-data` (`/data/hub.db`) | N/A для этого прогона: деплой bare-metal (см. H-D3) |
| H-D2 | Caddy-маршруты хаба: `https://DOMAIN/hub` (401 без сессии → логин), `/widget.js` (200), `/w`, `/join?t=…`, `/api/hub/health` (`{ok,enotdesk}`) | всё отвечается снаружи только через Caddy; напрямую `:8090` недоступен | 24–25.09.2026 на bare-metal: `/hub` 401 анониму, `/widget.js` 200, `/w` 200, `/api/hub/health` `{ok:true,enotdesk:true}`, `/join` без токена 404; WS `/ws/widget` — 101, `/ws/console` без сессии — 401 (после фикса маршрутов 1cb92c8/91026cd) |
| H-D3 | Bare-metal альтернатива: quick-setup (`--hub`/`--no-hub`; без флага — вопрос в TTY, без TTY честный отказ с подсказкой) или install-server.sh с `ENOT_HUB=1` | юнит `enotdesk-hub` (127.0.0.1:8090) + маршруты Caddy; `--update` не сносит хаб и сохраняет прежний выбор | 24.09.2026: quick-setup на чистой VPS (Ubuntu 24.04, root, `--tarball --domain support.ruenot.site --hub --admin-password-stdin`, неинтерактивно) — 4 службы active, health ok; `--update` не прогонялся |

### Прогон деплоя v0.2.1 (2026-09-24/25 — support.ruenot.site)

- Установка: bare-metal quick-setup, неинтерактивно (`--tarball` с фиксами маршрутов, т.к. релизный тарбалл v0.2.1 их ещё не содержит). DNS A-запись → 93.88.203.149 настроена до установки, LE-сертификат выпустился сразу. Сборки v0.2.1 (3 ОС) скачаны с GitHub Releases в `/var/lib/enotdesk/dist` — `/downloads` отдаёт все три.
- **Найдено и исправлено при живой приёмке** (коммиты в main, на сервере применено):
  1. `1cb92c8` — в bare-metal Caddyfile не было `/ws/widget /ws/console` → чат виджета и presence-консоль падали на основной сервер;
  2. `91026cd` — ни в bare-metal, ни в docker Caddyfile не было `/hooks/enotdesk` → webhook'и сервера в хаб всегда 404;
  3. `4d7d6fe` — TypeError «t is not a function» в консоли хаба (затенение i18n-`t` в `setThreadStatusLine`/`renderTags`): **любой клик по треду падал, тред не открывался** — найден только живым браузером, автотестами не покрывается;
  4. `f123196` (волна от 25.09 по живому тесту владельца) — три разрыва: (а) гость не видел свои сообщения в чате до перезагрузки (не было эха — теперь `guestAppend` пушит сообщение гостю); (б) оператор не мог подключиться к auto-claim сеансу one-click (claimId получал только хаб, пароля у оператора нет, повторный claim невозможен — добавлены `GET /sessions/:id/connect` + вход в /operator по ID без пароля); (в) **серверный тарбалл не вёз `web/` и `client/renderer`+`client/lib` — страница `/operator` на любых установках была сломана** (анониму 401 до чтения файла, залогиненному — падение чтения HTML); состав дополнен в deploy-server.sh/release.yml/allowlist install-server.sh, на проде файлы долиты.
- smoke-remote PASS сквозь Caddy (полный attended-цикл: логин → invite → сессия → WS relay → consent → offer/answer → end).
- Тестовая страница виджета на том же origin: `https://support.ruenot.site/widget-test/` (маршрут Caddy `handle_path /widget-test/*` → `/var/www/widget-test`; перезапись Caddyfile установщиком её сносит — восстановить при `--update`).
- Заведены аккаунты: admin + operator + auditor (пароли в локальном .env владельца, в репозитории не хранятся).

