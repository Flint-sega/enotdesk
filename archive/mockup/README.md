# archive/mockup

Архивный макет, не приложение. Приложение запускается командой `npm start` (Electron-клиент в `client/`).

Здесь лежат корневые `index.html`, `styles.css`, `app.js` — статичный визуальный макет раннего этапа. Он не собирается, не упаковывается и не используется сервером.
